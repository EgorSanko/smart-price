# Debug files
debug_*.py
check_*.py
test_*.py
scrape_*.py
smart_price_api*.py
smart_price_final.py
smart_price_scraper.py
smart_search.py
wb_api.py
*.html
*.json
!package.json
!package-lock.json
!tsconfig.json
!frontend/**/*.json
!.mcp.json

# Playwright MCP scratch (logs, snapshots, ad-hoc screenshots)
.playwright-mcp/

# Diploma screenshots — артефакты регрессионника, хранятся локально (не в git, чтобы репо не пух)
docs/diploma/screenshots/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
.next/

# Docker
*.log

# OS
.DS_Store
Thumbs.db

# Env (secrets — never commit)
.env
.env.*
!.env.example
!.env.local

# DBs (local SQLite + history)
*.db
*.sqlite
*.sqlite3

# TS build cache
*.tsbuildinfo

# Next.js build output
frontend/out/

# Backups
*.bak
*.bak.*

# Yandex Alisa session — NEVER commit
session/
